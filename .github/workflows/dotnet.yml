name: Build & Release

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  APP_NAME: TheBonding
  PROJECT: src/TheBonding.Maui/TheBonding.Maui.csproj

jobs:
  get-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.extract.outputs.version }}
    steps:
    - uses: actions/checkout@v4
    - name: Extract version from csproj
      id: extract
      run: |
        VERSION=$(grep -oP '(?<=<ApplicationDisplayVersion>)[^<]+' src/TheBonding.Maui/TheBonding.Maui.csproj | head -1)
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "App version: $VERSION"

  build-android:
    runs-on: ubuntu-latest
    needs: get-version
    env:
      VERSION: ${{ needs.get-version.outputs.version }}
    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Install MAUI Android workload
      run: dotnet workload install maui-android

    # 1. Restore MAUI for android — cascade will write lib assets with android TFM context
    - name: Restore MAUI (android)
      run: dotnet restore ${{ env.PROJECT }} -p:TargetFrameworks=net10.0-android

    # 2. Re-restore each lib individually — overwrites bad assets with correct net10.0 targets
    - name: Restore class libraries
      run: |
        dotnet restore src/TheBonding.Domain/TheBonding.Domain.csproj
        dotnet restore src/TheBonding.Shared/TheBonding.Shared.csproj
        dotnet restore src/TheBonding.Application/TheBonding.Application.csproj
        dotnet restore src/TheBonding.Infrastructure/TheBonding.Infrastructure.csproj

    # 3. Build libs with --no-restore so they use the correct net10.0 assets from step 2
    - name: Build class libraries
      run: |
        dotnet build src/TheBonding.Domain/TheBonding.Domain.csproj               -c Release --no-restore
        dotnet build src/TheBonding.Shared/TheBonding.Shared.csproj               -c Release --no-restore
        dotnet build src/TheBonding.Application/TheBonding.Application.csproj      -c Release --no-restore
        dotnet build src/TheBonding.Infrastructure/TheBonding.Infrastructure.csproj -c Release --no-restore

    # 4. Build MAUI only — valid MAUI android assets + valid lib net10.0 assets + pre-built lib DLLs
    - name: Build Android
      run: dotnet build ${{ env.PROJECT }} -p:TargetFrameworks=net10.0-android -c Release --no-restore --no-dependencies

    - name: Rename APK
      run: |
        mkdir -p artifacts
        cp src/TheBonding.Maui/bin/Release/net10.0-android/*.apk "artifacts/${{ env.APP_NAME }} - ${{ env.VERSION }}.apk"

    - name: Upload Android APK
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: artifacts/*.apk

  build-windows:
    runs-on: windows-latest
    needs: get-version
    env:
      VERSION: ${{ needs.get-version.outputs.version }}
    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Install MAUI Windows workload
      run: dotnet workload install maui-windows

    # 1. Restore MAUI for windows — cascade will write lib assets with windows TFM context
    - name: Restore MAUI (windows)
      run: dotnet restore ${{ env.PROJECT }} -p:TargetFrameworks=net10.0-windows10.0.19041.0 -r win-x64

    # 2. Re-restore each lib individually — overwrites bad assets with correct net10.0 targets
    - name: Restore class libraries
      run: |
        dotnet restore src/TheBonding.Domain/TheBonding.Domain.csproj
        dotnet restore src/TheBonding.Shared/TheBonding.Shared.csproj
        dotnet restore src/TheBonding.Application/TheBonding.Application.csproj
        dotnet restore src/TheBonding.Infrastructure/TheBonding.Infrastructure.csproj

    # 3. Build libs with --no-restore so they use the correct net10.0 assets from step 2
    - name: Build class libraries
      run: |
        dotnet build src/TheBonding.Domain/TheBonding.Domain.csproj               -c Release --no-restore
        dotnet build src/TheBonding.Shared/TheBonding.Shared.csproj               -c Release --no-restore
        dotnet build src/TheBonding.Application/TheBonding.Application.csproj      -c Release --no-restore
        dotnet build src/TheBonding.Infrastructure/TheBonding.Infrastructure.csproj -c Release --no-restore

    # 4. Build MAUI only — valid MAUI windows assets + valid lib net10.0 assets + pre-built lib DLLs
    - name: Build Windows
      run: dotnet build ${{ env.PROJECT }} -p:TargetFrameworks=net10.0-windows10.0.19041.0 -r win-x64 -c Release --no-restore --no-dependencies

    - name: Zip Windows build
      run: Compress-Archive -Path "src/TheBonding.Maui/bin/Release/net10.0-windows10.0.19041.0/win-x64/*" -DestinationPath "${{ env.APP_NAME }} - ${{ env.VERSION }}.zip"

    - name: Upload Windows build
      uses: actions/upload-artifact@v4
      with:
        name: windows-app
        path: "${{ env.APP_NAME }} - ${{ env.VERSION }}.zip"

  release:
    needs: [get-version, build-android, build-windows]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    permissions:
      contents: write
    env:
      VERSION: ${{ needs.get-version.outputs.version }}
    steps:
    - name: Download Android APK
      uses: actions/download-artifact@v4
      with:
        name: android-apk
        path: artifacts/android

    - name: Download Windows build
      uses: actions/download-artifact@v4
      with:
        name: windows-app
        path: artifacts/windows

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ env.VERSION }}-build.${{ github.run_number }}
        name: "${{ env.APP_NAME }} - ${{ env.VERSION }}"
        body: "Automated release from commit ${{ github.sha }}"
        files: |
          artifacts/android/*.apk
          artifacts/windows/*.zip
